# Claude Code guidance

Follow [AGENTS.md](AGENTS.md) as the canonical repository instructions.

When the user asks to install or configure this MCP, read `.agents/skills/install-oxoxos-cocos-game-assets-mcp/SKILL.md` and `docs/clients/claude-code.md`. Present the plan and wait for approval before persistent changes. Never ask the user to paste an OXOXOS token into chat.
