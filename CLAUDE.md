# Claude Code guidance

Follow [AGENTS.md](AGENTS.md) as the canonical repository instructions.

When the user asks to install or configure this MCP, read `.agents/skills/install-oxoxos-cocos-game-assets-mcp/SKILL.md` and `docs/clients/claude-code.md`. Present the plan and wait for approval before persistent changes. If the token is missing, give the portal onboarding steps; after the user intentionally provides a token in this trusted local installation session, configure it automatically without echoing it or placing it in commands, logs, source, or Git.
